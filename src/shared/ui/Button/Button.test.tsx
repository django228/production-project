import { Button, ThemeButton } from './Button';

// Simple test placeholder for beginner project
describe('Button', () => {
    test('Button component exists', () => {
        expect(Button).toBeDefined();
    });
});
